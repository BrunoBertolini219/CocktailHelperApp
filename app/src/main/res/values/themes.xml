<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base window theme applied before Compose draws. Material 3 + DayNight. -->
    <style name="Theme.CocktailHelperApp" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Splash screen via androidx core-splashscreen (works on API 21+). -->
    <style name="Theme.CocktailHelperApp.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.CocktailHelperApp</item>
    </style>

</resources>
